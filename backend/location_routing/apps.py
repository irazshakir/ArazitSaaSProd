from django.apps import AppConfig

class LocationRoutingConfig(AppConfig):
    default_auto_field = 'django.db.models.BigAutoField'
    name = 'location_routing'
    verbose_name = 'Location Based Lead Routing' 